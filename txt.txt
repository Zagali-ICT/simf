screen share Enable
src\Mobile\simf_app\android\app\src\main\kotlin\com\example\simf_app\MainActivity.kt

    override fun onCreate(savedInstanceState: Bundle?) {
        // TEMP-DEBUG-CAPTURE (owner-approved 2026-06-27): FLAG_SECURE disabled
        // ONLY to screenshot the device for visual verification. MUST be
        // restored before any commit / shipped build — this is an NCA control.
        // window.setFlags(
        //     WindowManager.LayoutParams.FLAG_SECURE,
        //     WindowManager.LayoutParams.FLAG_SECURE,
        // )
        super.onCreate(savedInstanceState)
    }

    










dotnet ef migrations add 20260712001 --context SimfAppDbContext   -p src/Backend/SIMF.Infrastructure/SIMF.Infrastructure.csproj   -s src/Backend/SIMF.Api/SIMF.Api.csproj   -o Persistence/Migrations/App

dotnet ef migrations add 20260712001 --context SimfIdentityDbContext   -p src/Backend/SIMF.Infrastructure/SIMF.Infrastructure.csproj   -s src/Backend/SIMF.Api/SIMF.Api.csproj   -o Persistence/Migrations/Identity





generate keys:
$bytes = New-Object byte[] 32
[System.Security.Cryptography.RandomNumberGenerator]::Fill($bytes)
[Environment]::SetEnvironmentVariable("SIMF_FileStorage__EncryptionKey",
    [Convert]::ToBase64String($bytes), "Machine")



    Restart-WebAppPool -Name SIMF.API   # or: net stop was /y ; net start w3svc  (or just re-run SIMF.Api.exe)


$env:SIMF_FileStorage__EncryptionKey = [Environment]::GetEnvironmentVariable("SIMF_FileStorage__EncryptionKey","Machine")
$env:SIMF_FileStorage__EncryptionKey.Length   # expect 44 now
.\SIMF.Api.exe

pwd : Qazwsx@1885


Email	Role
admin@simf.local	CP Administrator
vvip@simf.local	Visitor · VVIP
vip@simf.local	Visitor · VIP
visitor@simf.local	Visitor · Normal
staff@simf.local	Staff
moderator@simf.local	Moderator
exhibitor@simf.local	Exhibitor
media@simf.local	Media
sponsor@simf.local	Sponsor





PS D:\SIMF> Get-ChildItem C:\SIMF\Storage\logs\SIMF.Api\*.log |
>>   Sort-Object LastWriteTime | Select-Object -Last 3 |
>>   ForEach-Object { Select-String -Path $_.FullName -Pattern 'Super-admin' }





Purpose	Email	Password	Verified
زائر تجريبي (demo visitor, complete profile → goes to Home/My-Area)	visitor@simf.local	Kq7@Wm2xZ	✅ sign-in 200, no MFA; profile complete (ID image set, 3 interests)
Before-complete-profile (to test profile-data update)	vip@simf.local	Kq7@Wm2xZ	✅ Approved, but ID image MISSING + 0 interests → app routes to the profile-completion flow
Super admin (Control Panel)	superadmin@zagali-ict.com	Kq7@Wm2xZ	✅ sign-in 200, but it's a temp password → CP forces a change on first login (returns a passwordChangeToken); 2FA off
superadmin@zagali-ict.com / Kq7@Wm2xZ 


///fix AI
SET QUOTED_IDENTIFIER ON;   -- required, prod has filtered indexes
UPDATE dbo.AiPrompts SET Model = 'claude-haiku-4-5-20251001' WHERE Model = 'echo';







Session test case
Hall seat configuration — rows & columns layout, plus improve the seat-picking UI/UX (currently unclear)
Admin pre-reservation — admin reserves specific seats ahead of time (e.g. VIPs)
User reserves a seat — the visitor picks/reserves a seat
Random assignment — auto/random seat allocation that respects admin pre-reserved seats (doesn't collide with them)
User check-in to the hall
Cancel reservation with the 3-minute rule — (I need to confirm exactly what the 3-minute rule is — see below)
Ask questions (Q&A) once checked in
Rate the session at the end



For delgate also full test case
For speaker 
For G2G ad other meeting Testing



















:: on the server, elevated — download win-acme, then:
wacs.exe --target iis --host simf-api.zagali-ict.com --installation iis
Option B — self-signed with the correct SAN (what you asked; needs an app change too). Regenerate the cert with the right name, bind it, then I pin it in the app + rebuild:


# server, elevated PowerShell:
New-SelfSignedCertificate -DnsName "simf-api.zagali-ict.com","simf-cp.zagali-ict.com","simf.zagali-ict.com" `
  -CertStoreLocation "Cert:\LocalMachine\My" -FriendlyName "SIMF SAN" -NotAfter (Get-Date).AddYears(2)
# then IIS Manager → each site → Bindings → https → select this cert
Then I add a host-scoped network_security_config pin for simf-api.zagali-ict.com, rebuild, install, test. Caveat: this repo gitignores android/, so that pin isn't version-controlled — fine for the tablet build you run locally, but a CI/store build wouldn't include it. That's the other reason Option A is cleaner.

Which do you want? Whichever you pick, once the cert has SAN=simf-api, I'll re-point backgroundVideoUrl to the hyphen host and run the on-device test — and this time the hero should actually play. Option A gets there with no app rebuild at all.

# server, elevated PowerShell: