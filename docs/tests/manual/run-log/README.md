# Run-log — filled-in evidence (starts empty)

One file per **tester per day**, copied from the **Daily run-log template** in
[`../SIMF-Manual-Test-Plan-7Day.md`](../SIMF-Manual-Test-Plan-7Day.md) §10.1.

Naming: `day<N>-<tester>-lane<A|B|C>.md` (e.g. `day2-tester2-laneB.md`).

Also keep here (shared, one each):
- `defect-log.md` — the Defect Log (template §10.2).
- `cleanup-register.md` — the Cleanup Register (template §10.3) — **must be drained
  before sign-off; nothing `QA-`-tagged may remain in production.**

Screenshots/evidence go under `docs/screenshots/` (redact any QR / token / OTP).
