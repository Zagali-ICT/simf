# Pending global-doc merges — `exit-gate-no-open-high` (Track A, item A3)

Verification-only. The charter's exit criterion "no open high-severity defect"
was unmet because of the two findings held in
`docs/tests/SIMF-Round1-Held-Items-Plan.md`.

## DECISIONS_LOG

| D-NEXT | 2026-07-31 | **The Round-1 held-items exit gate is closed, and the held-items plan says so honestly (`exit-gate-no-open-high`).** `docs/tests/SIMF-Round1-Held-Items-Plan.md` held five findings, of which two blocked the charter's "no open high-severity defect" criterion: **#1** (demo accounts, incl. Administrator, seeded in every environment) and **#2** (Control-Panel sign-in not second-factor-enforced). #1 was **re-verified in code** this round, not taken from its status column — `IdentitySeeder.cs` gates `EnsureDemoAccountsAsync` behind `hostEnvironment.IsDevelopment() \|\| demoOptions.Value.EnableDemoAccounts` and logs a skip otherwise. #2 is implemented in this changeset (see the `#2` and `#2d` entries). The plan is updated in place with a dated status section rather than left reading "STILL DEFERRED", which is how a closed item silently becomes a stale one. **Three caveats are recorded in that section rather than asserted away:** (a) the consolidated build and full suite for this round are run once by the orchestrator at the end, so what is claimed is the tests written, not a green run pasted from this session; (b) the general integration suite pins `RequireControlPanelTwoFactorEnrolment` off, so the production posture is proved by a dedicated factory rather than by the whole suite; (c) the plan's "48 pre-existing failures" baseline contradicts a 2026-07-30 run reported as 2076 passed / 0 failed — that reconciliation is tracked separately as `48-pre-existing-failures` and is **not** closed by this update. | An exit gate closed by updating a status column is not closed. Re-reading the code for #1 and naming what is still unproven for #2 is what makes the claim worth anything. |

## PAGE-INDEX

No change — this item is documentation verification and touches no route.

## E2E-README

No change. `docs/tests/SIMF-Round1-Held-Items-Plan.md` is a plan document, not a
per-page catalogue file, so it is outside the `docs/tests/e2e/` index and the
`E2eCatalogueIntegrityTests` roll-up.
